// begin:tag time
// end:tag time
// begin:tag copilot_hints
/*
 * 入力例
 * - auto [n] = mtd::io::in<ll>();
 * - auto [a] = mtd::io::in<std::vector<ll>>(n);
 * - auto [n, m] = mtd::io::in<ll, ll>();
 * - auto [edges] = mtd::io::in<std::vector<std::tuple<ll, ll>>>(m);
 * - for(auto [n, m] : mtd::views::istream<ll, ll>(t)) { }
 *
 * 出力例
 * - std::cout << ans << std::endl;
 * - std::cout << std::fixed << std::setprecision(12) << ans << std::endl;
 * - for(auto x: ans) { std::cout << x << " "; } std::cout << std::endl;
 *
 * その他
 * - {} は常に付ける
 *
 */
// end:tag copilot_hints
//=============================================================================================
// begin:tag debug
#include "./Library/Debug/Dump.hpp"
#include "./Library/Debug/Test.hpp"
#include "./Library/Debug/Timer.hpp"
// end:tag debug
// begin:tag includes
#include "./Library/Main/includes.hpp"
#include "./Library/Range/util.hpp"
// end:tag includes
//=============================================================================================

using ll = long long;

signed main() {}
